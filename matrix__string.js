var ls = require('./list_functions.js');
var mm = require('./matrix__matrix.js');

/**
 * blah
 * @param {Matrix} m is a Matrix obj
 * @return {string}
 */
let str = (m) => {
  let toStringLength = (n) => (n.toString().length);
  let lengthList = m.getColumns().map(toStringLength);

  let printElement = (l) => (5);
};
console.log(leftpad([2].toString(), 5));
