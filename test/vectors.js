const List = require('immutable').List

const vectors = {
  z1: List([0]),
  z2: List([0, 0]),
  z3: List([0, 0, 0]),

  o1: List([1]),
  o2: List([1, 1]),
  o3: List([1, 1, 1])
}

module.exports = vectors
