const util = require('.').util
const List = require('immutable').List
const slash = '/'
const F = require('mathjs').fraction
const v = require('.').vectors
console.log(util.vectorToArrayVector(v.s3))
